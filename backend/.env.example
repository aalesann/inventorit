# ==============================================
# CONFIGURACIÓN DEL SERVIDOR
# ==============================================

# Puerto del servidor backend
PORT=3000

# IP del servidor (para CORS y logging)
# En desarrollo: localhost
# En producción: la IP del servidor on-premise (ej: 10.0.2.10)
SERVER_IP=localhost

# Orígenes permitidos para CORS (separados por comas)
# Dejar vacío para usar la configuración por defecto basada en SERVER_IP
# Para permitir múltiples orígenes específicos:
# ALLOWED_ORIGINS=http://10.0.2.10,http://10.0.2.10:80,http://otro-servidor.com
# ⚠️ ADVERTENCIA: NO usar * en producción
ALLOWED_ORIGINS=

# ==============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==============================================

DB_HOST=db
DB_USER=postgres
DB_PASSWORD=postgrespassword
DB_NAME=inventory_db
DB_PORT=5432

# ==============================================
# CONFIGURACIÓN DE ENTORNO
# ==============================================

# Entorno de ejecución (development, production)
NODE_ENV=development

# ==============================================
# CONFIGURACIÓN DE SEGURIDAD - JWT
# ==============================================

# Secret para JWT (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Duración del access token (recomendado: 15m)
JWT_ACCESS_EXPIRATION=15m

# Duración del refresh token (recomendado: 7d)
JWT_REFRESH_EXPIRATION=7d

# ==============================================
# CONFIGURACIÓN DE SEGURIDAD - LOGIN ATTEMPTS
# ==============================================

# Número máximo de intentos de login fallidos antes de bloquear
MAX_LOGIN_ATTEMPTS=5

# Duración del bloqueo en minutos
BLOCK_DURATION_MINUTES=15

# ==============================================
# CONFIGURACIÓN DE SEGURIDAD - RATE LIMITING
# ==============================================

# Ventana de tiempo para rate limiting (en milisegundos)
# 15 minutos = 900000 ms
RATE_LIMIT_WINDOW_MS=900000

# Número máximo de requests por ventana (global)
RATE_LIMIT_MAX_REQUESTS=100

# Número máximo de intentos de login por ventana
RATE_LIMIT_LOGIN_MAX=5

# ==============================================
# CONFIGURACIÓN DE USUARIO ADMIN POR DEFECTO
# ==============================================

# Contraseña del usuario admin por defecto
# IMPORTANTE: Cambiar inmediatamente después del primer login
DEFAULT_ADMIN_PASSWORD=Adm1n$ecur3!2024
